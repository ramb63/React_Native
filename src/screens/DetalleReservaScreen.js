import { useEffect, useState } from 'react';
import { ActivityIndicator, StyleSheet, Text, View } from 'react-native';
import { getDatabase } from '../database';
export default function DetalleReservaScreen({ route }) { const [reserva, setReserva] = useState(null); useEffect(() => { getDatabase().then((db) => db.getFirstAsync('SELECT * FROM reservas WHERE id = ?', route.params.id)).then(setReserva); }, [route.params.id]); if (!reserva) return <ActivityIndicator style={styles.loading} />; return <View style={styles.page}><Text style={styles.title}>{reserva.cliente}</Text><Text style={styles.status}>{reserva.estado}</Text><View style={styles.card}><Text>Cancha {reserva.cancha}</Text><Text>{reserva.fecha}</Text><Text>{reserva.horaInicio} - {reserva.horaFin}</Text><Text>Anticipo: $ {reserva.anticipo}</Text><Text>{reserva.observaciones || 'Sin observaciones'}</Text></View></View>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: '#f4f7f9', padding: 20, gap: 16 }, loading: { flex: 1 }, title: { fontSize: 32, fontWeight: '900', color: '#102a43' }, status: { color: '#0ea5a4', fontWeight: '800' }, card: { backgroundColor: '#fff', borderRadius: 14, padding: 18, gap: 14 } });
